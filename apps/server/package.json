{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "go run cmd/main.go",
    "install": "go mod tidy",
    "build": "go build",
    "test": "go test ./...",
    "generate:openapi": "swag init -g cmd/main.go -o docs/"
  }
}
